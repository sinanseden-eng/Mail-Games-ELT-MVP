# 0.9H4B — Broadcast Authority

The active browser chain uses unique 0.9H4B filenames from HTML through the scene and visual renderer. The exact uploaded broadcast photograph is the startup and replay plate, preventing old cached penalty modules from taking control.
