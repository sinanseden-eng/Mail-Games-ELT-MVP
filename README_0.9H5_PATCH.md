# 0.9H4D → 0.9H5 Patch

Apply this patch only to a repository already containing the complete 0.9H4D build.

Copy everything inside the patch folder into the repository root and replace matching files. Then use Netlify's **Clear cache and deploy site** option.

The patch does not require a Supabase migration or new environment variables.
