# 0.9H4B → 0.9H4C Patch

Extract the patch and copy its contents into the root of an existing 0.9H4B repository. Preserve directories and replace matching files. Then use Netlify's Clear cache and deploy site option.

The complete 0.9H4C ZIP is recommended because it guarantees that the new entry-module chain and both broadcast plates are present together.
